{
  "name": "project4",
  "version": "1.0.0",
  "description": "Using this as endpoint in my project4.sales users tries to share the opportunity records with external systems like this.So we use this to receive those records and acknowledge with a success response.",
  "main": "hello-world.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start" : "node hello-world.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "ngrok": "^3.3.0"
  }
}
